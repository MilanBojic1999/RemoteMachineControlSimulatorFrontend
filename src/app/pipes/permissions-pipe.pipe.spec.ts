import { PermissionsPipePipe } from './permissions-pipe.pipe';

describe('PermissionsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PermissionsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
